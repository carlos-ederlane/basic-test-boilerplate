{
  "name": "front-end-test-2",
  "version": "1.0.0",
  "description": "Simple Parcel Sandbox",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html",
    "deploy": "yarn build && surge",
    "scss": "node-sass ./assets/scss --watch --output ./assets/css"
  },
  "dependencies": {
    "parcel": "1.12.4",
    "surge": "^0.23.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.6.1",
    "bootstrap": "^4.5.2",
    "node-sass": "^4.14.1",
    "vanilla-masker": "^1.2.0"
  },
  "keywords": []
}
